server {
    listen 80;

    # Project 1 (default)
    location / {
        proxy_pass http://project1:5173;
    }

    # Project 2
    location /project2/ {
        proxy_pass http://project2:5174;
    }

    # Backend API
    location /api/ {
        proxy_pass http://backend:3001;
    }
}
